# Drafts live here. Each weekly routine run adds one `YYYY-MM-DD.md`.
# These committed drafts double as the "already covered" memory the routine
# dedupes against. (Local send previews `sent-*.html` are gitignored.)
